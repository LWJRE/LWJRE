package io.github.ardentengine.core.display;

/**
 * Enum used to change the window mode.
 *
 * @see DisplayServer#setWindowMode(WindowMode)
 */
public enum WindowMode {
    WINDOWED,
    MAXIMIZED,
    MINIMIZED,
    FULLSCREEN
}